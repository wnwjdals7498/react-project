{
  "compilerOptions": {
    "target": "es5",                          // 컴파일된 JavaScript 버전
    "module": "commonjs",                     // 모듈 시스템 (CommonJS, AMD, System, ... 등)
    "strict": true,                           // 엄격한 타입 체크
    "esModuleInterop": true,                  // ES 모듈 호환성
		"skipLibCheck": true,                     // 라이브러리 검사 건너뛰기
    "forceConsistentCasingInFileNames": true, // 파일 이름의 일관된 대소문자
    "outDir": "./dist",                       // 컴파일된 JavaScript 파일 출력 경로
    "rootDir": "./src"                        // TypeScript 소스 코드 경로
  },
  "include": [
    "src/**/*.ts"                            // 컴파일할 TypeScript 파일들의 경로
  ],
  "exclude": [
    "node_modules"                           // 컴파일에서 제외할 디렉토리
  ]
}